// src/components/Header.js
import React from 'react';
 // Import the CSS file for styling (if it's separated)

const Header = () => {
  return (
    <header>
      <div className="company-name">WeHeal</div>
    </header>
  );
};

export default Header;
